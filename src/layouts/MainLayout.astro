---
const { title = "Better Health Facts" } = Astro.props;
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{title}</title>
  <meta name="description" content="Scientific health research and evidence-based medical articles." />
  <style>
    :root {
      --primary: #0f766e;
      --bg: #ffffff;
      --text: #1f2937;
    }

    body {
      margin: 0;
      font-family: system-ui, -apple-system, sans-serif;
      background: var(--bg);
      color: var(--text);
    }

    a {
      text-decoration: none;
      color: var(--primary);
    }

    header {
      background: var(--primary);
      color: white;
      padding: 15px 20px;
    }

    nav a {
      color: white;
      margin-right: 15px;
      font-weight: 500;
    }

    main {
      max-width: 900px;
      margin: auto;
      padding: 20px;
    }

    footer {
      background: #f3f4f6;
      padding: 20px;
      text-align: center;
      font-size: 14px;
      margin-top: 40px;
    }

    .card {
      border: 1px solid #e5e7eb;
      padding: 20px;
      border-radius: 8px;
      margin-bottom: 20px;
      transition: 0.2s ease;
    }

    .card:hover {
      box-shadow: 0 5px 15px rgba(0,0,0,0.08);
    }

    @media (max-width: 600px) {
      main {
        padding: 15px;
      }
    }
  </style>
</head>

<body>

<header>
  <strong>Better Health Research</strong>
  <nav>
    <a href="/">Home</a>
    <a href="/articles/">Articles</a>
    <a href="/about/">About</a>
    <a href="/disclaimer/">Disclaimer</a>
  </nav>
</header>

<main>
  <slot />
</main>

<footer>
  © {new Date().getFullYear()} BetterHealthFacts Research.
</footer>

</body>
</html>
